import { MD3DarkTheme as DefaultTheme } from 'react-native-paper';

const theme = {
  ...DefaultTheme,
  colors: {
    ...DefaultTheme.colors,
    primary: '#7C3AED',
    secondary: '#4F46E5',
    background: '#0B0B12',
    surface: '#101322',
    onPrimary: '#FFFFFF',
  },
};

export default theme;